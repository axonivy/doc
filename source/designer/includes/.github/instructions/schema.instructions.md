---
name: JSON and YAML Schema Conventions
description: "Use when creating or editing JSON or YAML files with a schema reference."
applyTo: "**/*.json, **/*.yaml
---

- Respect the `$schema` reference (or YAML schema comment), and fetch it when necessary before editing.
